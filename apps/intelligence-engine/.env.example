# Required in production; falls back to a logged insecure default in dev.
JWT_SECRET=

# Comma-separated allowlist. Defaults to public-map/citizen-app/authority-portal dev ports if unset.
CORS_ORIGINS=http://localhost:3000,http://localhost:3002,http://localhost:3003

NODE_ENV=development
