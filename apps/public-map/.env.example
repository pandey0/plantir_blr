# Copy to .env.local for local development (gitignored). Defaults to
# http://localhost:3001 in lib/api.ts if unset, so this file is optional for the
# default local dev setup — only needed to point at a different engine instance.
NEXT_PUBLIC_API_URL=http://localhost:3001
