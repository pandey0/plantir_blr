# API Reference — intelligence-engine

> **Ownership rule**: add/change/remove a route in `apps/intelligence-engine/src/index.ts` (or, once it exists, `routes/manifest.ts`) → update this doc in the same change.
>
> **Status**: hand-maintained (CURRENT). Once `proto/plantir/**/v1/*.proto` + `routes/manifest.ts` exist (see [`../architecture/DATA_FLOW.md`](../architecture/DATA_FLOW.md)), this will be generated by a `docs:api` script from those two sources.

Base URL: `http://localhost:3001` (single instance, no `/v1` prefix yet — versioning is PLANNED alongside the proto migration).

## Auth (CURRENT — Phase 0 done)

JWT bearer tokens (`@fastify/jwt`), role claim `'citizen' | 'authority'`. `Authorization: Bearer <token>` header. No user accounts exist yet — in non-production environments, `POST /dev/token` mints a token for either role for local testing. **There is no production-safe way to issue a token today** — a real login/identity flow for citizen-app and authority-portal is still unbuilt; `/dev/token` is explicitly not it (see "Not yet built" below).

`JWT_SECRET` env var: required in production (server refuses to start without it); falls back to a logged insecure default in dev.

## Routes

### `GET /health`
`{ status: 'ok' }`. No auth, no DB check — just confirms the process is up.

### `GET /ws`
WebSocket upgrade. Server pushes `{ type: 'NEW_EVENT', payload: EventPayload }` to every connected client whenever an event is created (via `POST /report` or, in non-production, `POST /dev/inject`). No client→server messages are consumed, no auth. See [`../../apps/intelligence-engine/src/ws/README.md`](../../apps/intelligence-engine/src/ws/README.md).

### `GET /events?cursor=&limit=`
Public, no auth. Returns events where `status != 'FRAUD'`, ordered `created_at desc, id desc`. Cursor-paginated: `limit` (1–100, default 50), `cursor` (an event `id`, optional). Response: `{ events: Event[], nextCursor: string | null }`. `nextCursor` is `null` on the last page.

### `PATCH /events/:id/status`
**Auth: `authority` role required.** Body: `{ status: EventStatus }` (`REPORTED | VERIFIED | ESCALATED | IN_PROGRESS | RESOLVED | FRAUD`), both `id` (must be a UUID) and `status` Zod-validated, 400 on failure. Still no validation that the *transition* is legal (e.g. `RESOLVED → REPORTED` is accepted if the caller has an authority token) — not addressed in Phase 0, tracked as a follow-up.

### `POST /report`
**Auth: `citizen` or `authority` role required.** Body (Zod-validated): `{ latitude: number [-90,90], longitude: number [-180,180], category: EventCategory, location?: string }`. Creates an `Event` with `confidence_score: 10` (still hardcoded — confidence scoring itself is PLANNED, see [`../../apps/intelligence-engine/src/events/README.md`](../../apps/intelligence-engine/src/events/README.md)), **persists coordinates to the `geom` column** via `createEventWithGeom()` (raw SQL `ST_SetSRID(ST_MakePoint(...))`), and broadcasts `NEW_EVENT` over WS.

### `POST /dev/token` — non-production only, route doesn't exist when `NODE_ENV=production`
Body: `{ role: 'citizen' | 'authority' }`. Returns `{ token }`, a JWT signed for that role, 2h expiry. This is a development convenience for exercising auth-gated routes before a real login flow exists — **not a login endpoint**, has no concept of a user identity, anyone can mint a token for either role in any non-production environment this is deployed to (staging included). Do not expose a non-production build of this service publicly.

### `POST /dev/inject` — non-production only, route doesn't exist when `NODE_ENV=production`
No body, no auth (gated by environment instead). Generates a random category/location/coordinates within Bangalore's bounding box, creates an event with persisted `geom`, broadcasts it.

### `GET /transit/arrivals?station=&mode=METRO|BUS`
Public, no auth. Returns `ArrivalData[]`. **Fully mocked** — random ETAs, not a real transit feed. See [`../../apps/intelligence-engine/src/transit/README.md`](../../apps/intelligence-engine/src/transit/README.md).

### `GET /transit/estimate?from=&to=&mode=METRO|BUS`
Public, no auth. Returns `{ fare: number, time: string }`. `fare` is random; `time` is a hardcoded string (`'28 mins'`) regardless of input. Mocked, same caveat as above.

## CORS

Explicit origin allowlist (was previously wildcard) via `CORS_ORIGINS` env var, comma-separated. Defaults to `http://localhost:3000,http://localhost:3002,http://localhost:3003` (public-map, citizen-app, authority-portal dev ports).

## Not yet built (tracked, not implemented)

- Real login/identity — `/dev/token` is a placeholder, not a login flow. Building this is a bigger scope item (likely touches auth architecture) — check [`../architecture/TECH_STACK.md`](../architecture/TECH_STACK.md)'s "what counts as major" before starting it.
- Status-transition validation on `PATCH /events/:id/status`.
- bbox/viewport filtering on `GET /events` (geom is now persisted, so this is unblocked, just not built yet).

## Planned (PLANNED — not yet true)

- All routes move under `/v1/` once `proto/plantir/**/v1/*.proto` lands.
- Full request/response types generated from `packages/api-contracts` rather than hand-typed here.
