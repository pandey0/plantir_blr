# Tech Stack & Architecture Rules

> **Ownership rule**: this doc is updated continuously as the project evolves — same change as the code, no exceptions, same as every other doc in this tree (see [`../README.md`](../README.md)).
>
> **Change policy — read before proposing a major change**: routine changes (new route, new field, new adapter, bugfix, new small dependency) get documented here or in the relevant module doc in the same change, no sign-off needed. **Major changes require explicit user confirmation before implementation** — see "What counts as major" below. Propose it, mark it `PROPOSED` in the table, wait for a go-ahead, then implement and flip the status.

## Core principles

- **Modular monolith first.** Extract a separate service only when a real scale number forces it, not speculatively.
- **No Kubernetes, microservices, Kafka, event sourcing, or CQRS** until scale actually demands them. Not on the table at current size.
- **One always-on stateful service** (`intelligence-engine` — holds WS connections + DB pool) vs. **stateless/edge-friendly frontends**. Don't blur this line without discussion.
- **Don't add infra ahead of the trigger that justifies it** — e.g. Redis pub/sub only once we're actually running >1 engine instance, not because it "seems like the scalable thing to do." See [`DATA_FLOW.md`](DATA_FLOW.md) for the specific trigger conditions already identified.
- **Docs precede implementation** for new modules/sources — write the module's `README.md` (purpose, interface, data flow) before the code, per [`../README.md`](../README.md).

## Related repos

- **`plantir-blr-data-service`** (sibling directory to this monorepo, `../plantir-blr-data-service`) — FastAPI/Python. Serves Bangalore geo reference data (BBMP wards, metro lines/stations) and transit data (arrivals, fare estimates) behind a provider interface (`TransitProvider`/`GeoProvider`) designed so a real upstream can be plugged in later without changing this monorepo's code. `intelligence-engine`'s `transit.ts` is its only consumer today (2026-08-10). See that repo's own `README.md`/`CLAUDE.md`. **Its own repo, own git history, own dependency stack (Python) — not a workspace of this monorepo.**

## Current stack, per app/package

| App/package | Stack | Status |
|---|---|---|
| `apps/intelligence-engine` | Fastify 5, TypeScript (ESM, `.js` import extensions required), Prisma, PostgreSQL+PostGIS, Redis (`ioredis` — provisioned, not yet used), Zod, axios (now genuinely used — see `transit.ts`, calls `plantir-blr-data-service`) | CURRENT |
| `apps/public-map` | Next.js 14 (App Router), React 18, Leaflet + react-leaflet, @turf/turf, Tailwind, Radix UI, lucide-react | CURRENT |
| `apps/citizen-app` | Vite + React, TypeScript | Stub only |
| `apps/authority-portal` | Vite + React, TypeScript | Stub only |
| `packages/database` | Prisma schema + migrations (raw SQL for PostGIS-specific pieces) | CURRENT |
| `packages/api-contracts` | Zod schemas + `ts-proto`-generated types from `proto/`. Builds clean, not yet consumed by any route — see its own [`README.md`](../../packages/api-contracts/README.md) for why. | CURRENT (scaffolded, unconsumed) |
| `proto/` | Protocol Buffers, `buf` CLI (npm-distributed via `@bufbuild/buf`, no Go toolchain) + `ts-proto` for TS codegen (`stringEnums=true`, `outputServices=false`). Explicitly **not** using worldmonitor's `sebuf` (its plugins are Go-built; decided against adding a Go toolchain to this Node-only monorepo — see decision log below). No lint/breaking-change enforcement yet (`proto/buf.yaml` is deliberately minimal). | CURRENT — `events` and `transit` domains defined |

## Decision log

Chronological, newest first. Every entry here was either a direct user call or is flagged `PROPOSED` awaiting one.

| Date | Decision | Why |
|---|---|---|
| 2026-08-10 | **New sibling repo**: `plantir-blr-data-service` (FastAPI/Python), scaffolded to hold Bangalore transit + geo reference data behind a provider interface (`TransitProvider`/`GeoProvider`), so a real upstream (BMTC/BMRCL feeds, a government open-data API) can be plugged in later as a new provider class with zero downstream changes — same idea as `ingestion/`'s `Source` adapter pattern in this repo, applied to a service that didn't exist before. `intelligence-engine/src/transit.ts` rewritten from self-contained mock logic to a thin HTTP client (`axios`, 5s timeout, no silent fallback — a data-service outage is now a visible 500, not empty data) calling this service; `calculateFare()` renamed `getFareEstimate()` (now async, returns `{fare, time}` instead of just a fare number — the hardcoded `time: '28 mins'` in `app.ts` is gone). New `DATA_SERVICE_URL` config var, defaults to `http://localhost:8000`. | Direct user instruction — confirmed via two clarifying questions (repo: scaffold new vs. use existing; scope: static-map-data-only vs. broader live-data ingestion) before building, since "new datastore/infra/service" is explicitly "major" per this table's own criteria. User chose: scaffold new, broader scope (geo + bus + metro + real-time + pricing, architected for easy real-API integration later). |
| 2026-08-10 | `public-map` adapted to the engine's `/v1` API (its first real engineering pass since being scaffolded) — closed the actual production break where 4 components hardcoded pre-`/v1` unversioned paths against `http://localhost:3001`, silently 404ing since the `/v1` migration. New `lib/api.ts` centralizes all engine calls (base URL via `NEXT_PUBLIC_API_URL`); `page.tsx` now hydrates events via `GET /v1/events` on load (previously only ever learned about events live via WS, so a refresh showed nothing) and handles `EVENT_UPDATED` WS messages (previously silently ignored). Also fixed 3 CommuteDrawer tabs (Rail/BMTC/KSRTC) whose `apiMode` values (`'RAIL'`/`'BMTC'`/`'KSRTC'`) never matched the engine's actual `TransitMode` enum (`METRO`\|`BUS`) — these always 400'd, silently masked by a catch-and-show-dashes fallback, a pre-existing bug unrelated to versioning. First formal audit against `docs/standards/frontend-engineering-standards.md` landed alongside — see `PUBLIC_MAP_STANDARDS_COMPLIANCE.md`. New app-level `apps/public-map/CLAUDE.md` + `README.md` (this app had zero docs before this session). | User directive to build out `public-map` following the same docs/standards rigor as the engine, verified end-to-end in Chrome (hydration, live WS push via `/dev/inject`, transit fare estimate). |
| 2026-08-10 | Closed remaining backend-standards gaps: `Idempotency-Key` support on `POST /v1/events` (`events/idempotency.ts`, in-memory dedup-key store, 24h TTL — same "no Redis yet, single instance" reasoning as `list-cache.ts`), 4 new DB indexes (`Event.created_at`, `Event(category,status,created_at)`, `Report.event_id`, `Evidence.event_id` — the last two are real gaps, Postgres doesn't auto-index FK columns), an IDOR/BOLA analysis (conclusion: no gap, no per-user-owned resource exists yet), and a secrets-in-logs audit (conclusion: no secret is ever logged; found and fixed one unrelated dead-code issue in `transit.ts`). See `STANDARDS_COMPLIANCE.md`'s 2026-08-10 summary and `IMPLEMENTATION_NOTES.md` for each. | User directive to close remaining engine gaps after the VISION-gap round, following the standards docs read in that same round (`authentication-security-`, `sql-database-`, `software-engineering-practices-engineering-standards.md`). |
| 2026-08-10 | **Reverses** the 2026-08-09 confidence-formula decision: replaced the flat additive formula with VISION.md's multi-signal model (Confidence Engine v2 — diminishing-returns reporter signal, capped evidence signal, authority-confirmation bonus, unchanged FRAUD penalty). Spatial/temporal signals are expressed as the *eligibility gate* for duplicate/corroboration detection (below), not separate additive terms. Weights are documented defaults, explicitly not calibrated — user confirmed calibration is deferred to beta, not guessed pre-beta. | Direct user confirmation (asked explicitly given this reverses a logged decision) in response to a VISION.md gap review. See `events/README.md` for the full formula and `VISION.md`'s Event Confidence Engine section (divergence note now resolved). |
| 2026-08-10 | Duplicate/corroboration detection landed in `createEvent()`: a same-category report within 150m and 6 hours of an existing non-terminal event attaches to it as a new `Report`/`Evidence` instead of creating a second `Event`. Runs inside the same `$transaction` as the write. **Known residual race, not solved**: two fully concurrent `createEvent()` calls in overlapping transactions can both miss each other under Postgres's default READ COMMITTED isolation (neither transaction sees the other's uncommitted row yet) — would need row/advisory locking to close, not justified at this app's traffic. GPS validation also landed: `POST /v1/events` rejects coordinates outside a generous Bangalore-metro bounding box. Both are VISION.md Fraud Prevention items; camera-only uploads, user reputation scoring, and AI media verification remain unbuilt (each needs a real product decision — upload pipeline, account system, AI provider — not just engine code). | Same VISION.md gap review as the confidence-formula entry above. See `events/geo-query.ts`'s `findDuplicateCandidateEventId()` and `packages/api-contracts/src/schemas.ts`'s `createEventRequestSchema`. |
| 2026-08-10 | Three new read endpoints landed: `GET /v1/events/heatmap` (reuses clustering's grid aggregation, reinterpreted as weighted points), `GET /v1/events/playback?from=&to=&bbox=` (time-windowed event history, capped at 30 days/1000 rows, no cursor), `GET /v1/events?wardId=` (BBMP ward polygon filtering — engine keeps its own copy of `bbmp-wards.json`, no cross-app import, no new `Ward` table; the polygon is passed as a query-time parameter into `ST_GeomFromGeoJSON`, not persisted/seeded). All three are additive, no breaking changes to existing routes. | Closes the remaining VISION.md engine-side gaps identified in the same review (heatmap generation, playback/temporal query, ward-based filtering) that this session's `/v1` and clustering work hadn't yet covered. See `apps/intelligence-engine/src/wards/README.md` and `docs/api/intelligence-engine.md`. |
| 2026-08-09 | CI added (`.github/workflows/ci.yml`): two jobs matching the unit/integration test split — unit tests + typecheck + `docs:api:check` with no external services, integration tests against a real `postgis/postgis` service container using `prisma migrate deploy` (first use of that command in this repo — verified locally). | Closes `docs/standards/backend-engineering-standards.md` Section 33's explicit CI requirement, part of the same standards-compliance pass as the entry below. |
| 2026-08-09 | Adopted `docs/standards/{backend,frontend}-engineering-standards.md` as binding, user-authored. Backend audited against `intelligence-engine` in `docs/architecture/STANDARDS_COMPLIANCE.md`; real gaps found and fixed (transaction safety, controller/DB-query separation, error response shape, env validation, security headers, graceful shutdown, CI). Frontend standards wired into the doc index but not applied yet — no frontend app is being touched this session. | User directive. Reverses one earlier documented convention: `events/README.md` previously said "reads don't need to go through `events/`" for `GET /v1/events` — the standards' "controllers must not contain direct DB queries" rule overrides that, logged as its own compliance-doc entry. |
| 2026-08-09 | Protobuf via `buf` + `ts-proto` (Node-only), not `sebuf` | `sebuf`'s HTTP-annotation-driven codegen (used by reference project worldmonitor.app) requires Go-built plugins. User chose to keep the toolchain pure-Node rather than add Go for codegen only. Cost: we hand-maintain the RPC→HTTP path/method mapping (`routes/manifest.ts`) instead of getting it generated from proto annotations. |
| 2026-08-09 | Restructure focuses on `intelligence-engine` only; `public-map`/`citizen-app`/`authority-portal` untouched for now | User confirmed scope — matches the "data coming in from other sources" framing being a backend/ingestion concern. |
| 2026-08-09 | Docs precede code; every module/component/API gets a doc, updated in the same change as the code it describes; major architecture changes need explicit confirmation before implementation | User directive — this file and [`../README.md`](../README.md) exist to enforce it. |
| 2026-08-09 | Phase 0 landed: `@fastify/jwt` role-based auth (`citizen`/`authority`) on mutating routes, `geom` migration + persisted writes, Zod validation, cursor pagination on `GET /events`, CORS allowlist, `/health`. Not logged as a separate `PROPOSED` item — this was already-agreed scope from the architecture review, not a new major decision. | Closes the Phase 0 gaps identified during the architecture skill review (open `/dev/inject`, unpersisted coordinates, unbounded `GET /events`, wildcard CORS). See [`../api/intelligence-engine.md`](../api/intelligence-engine.md) for what's actually enforced. `/dev/token` is an explicit placeholder, not a real login flow — building one is a separate, larger decision. |
| 2026-08-09 | Phase 1 landed: `proto/plantir/{events,transit}/v1/*.proto`, `buf`+`ts-proto` toolchain, `packages/api-contracts` generated + building. `routes/manifest.ts` written (documents current routes, not yet consumed by `index.ts`'s registration). Current unversioned routes intentionally left on `@prisma/client` enums, not switched to the generated ones — see `packages/api-contracts/README.md` "Not yet wired in" for the TS nominal-typing reason. | Continuation of the already-agreed proto adoption, not a new major decision. The cutover to `/v1` routes using these generated types is the next increment, deliberately not bundled into this one to avoid an unnecessary type-cast at the Prisma boundary for zero behavior change. |
| 2026-08-09 | Phase 2 landed: `events/`, `ws/`, `ingestion/` extracted from `index.ts` per their already-written specs, wired in (route *handlers* now call these modules; route *registration* stays inline — `routes/`'s extraction is still separately deferred). `citizen-report` is the first real `Source` adapter. One behavior change beyond pure extraction: `PATCH /events/:id/status` still does not broadcast — flagged, not bundled in, since a new WS payload shape needs a `public-map`-side change and this restructure stays engine-only. | Matches the already-agreed module boundaries from `events/README.md`/`ws/README.md`/`ingestion/README.md`, not a new decision. Added a shared `db.ts` Prisma singleton (wasn't in the original module docs) to avoid each new module opening its own connection pool — small, uncontroversial, documented in `src/README.md`. |
| 2026-08-09 | Testing: **Vitest** (not Jest), two tiers — `test:unit` (no DB, runs in pre-commit) / `test:integration` (real Postgres via Fastify `.inject()`, not in pre-commit). Extracted `app.ts` (`buildApp()`) from `index.ts` so tests don't bind a real port — Fastify's own recommended testing pattern. **Husky** pre-commit added: typecheck + `test:unit` + a non-blocking doc-update reminder script (`scripts/check-docs-reminder.mjs`) that warns (doesn't fail) when source changes with no doc/README staged in the same commit. | Not "major" per this table's own criteria (no infra/framework swap, easily reversible), so implemented directly rather than proposed first — but logged here since it's a real standing convention now, see `TESTING.md`. Docs-reminder is a warning, not a block, because a hard gate on "did docs change" produces false positives (refactors, typo fixes) that just train `--no-verify`, which is worse than no gate. |
| 2026-08-09 | User directive: build `intelligence-engine` to be fully correct/complete per `docs/product/VISION.md` and the engine's own docs, without waiting for or protecting compatibility with `public-map`/`citizen-app`/`authority-portal` — those apps adjust to the engine on their own turn, not the reverse. Confirmed specifically for the `/v1` migration (breaks `public-map`'s current calls, not fixed now) and status-update broadcasting (new WS message type `public-map` doesn't render yet, confirmed harmless — its handler ignores unrecognized types). | Direct user instruction, changes the posture `OVERVIEW.md`/`DATA_FLOW.md` had been documenting ("engine-only scope, don't break other apps" read as "don't touch them," not "never let engine correctness diverge from them"). |
| 2026-08-09 | `@fastify/rate-limit` added, `POST /v1/events` limited to 10/min per client IP (not global — other routes unaffected). Real bug hit and fixed while building this: route registration had to move inside `fastify.after(...)` because the plugin's per-route `config.rateLimit` is picked up by an `onRoute` hook installed asynchronously during the plugin's own registration — declaring routes synchronously right after `fastify.register(rateLimit, ...)` raced it and silently no-opped the limit (no error, every request just succeeded). Caught by a dedicated integration test, not by inspection. | Not "major." Full writeup: [`IMPLEMENTATION_NOTES.md`](IMPLEMENTATION_NOTES.md#rate-limiting). |
| 2026-08-09 | **Pivoted** the planned `docs:api` generation script: originally scoped (in `routes/README.md`) as fully regenerating `docs/api/intelligence-engine.md` from `routes/manifest.ts` + proto comments. Implemented instead as a **drift check** (`apps/intelligence-engine/scripts/check-api-docs.ts`) that verifies every manifest route has a documented heading, without touching the doc's content. Reason: that doc has since accumulated substantial hand-written value this session (known bugs, cross-references, auth caveats, testing notes) that a bare manifest-driven generator can't reproduce — fully overwriting it would be a regression, not the intended improvement. | Not "major" (doesn't change any decision, just the tooling approach for keeping one doc honest) — but flagged explicitly since it reverses something written down elsewhere (`routes/README.md`), not a silent scope change. |
| 2026-08-09 | Request-coalescing cache landed for `GET /v1/events` (`events/list-cache.ts`) — in-memory `Map`, no Redis, 3s TTL, 200-entry cap with FIFO eviction. Deliberately invalidated **entirely on every event write** (`createEvent()`/`updateStatus()` both call `invalidate()`), not left to expire on TTL alone — a citizen/authority re-querying right after their own write must see it, and this app has live WS push for real-time anyway so the cache exists to absorb read bursts, not to serve intentionally-stale data. This is the adapted (not copied) version of the worldmonitor `cachedFetchJson()` idea already tracked in `REFERENCES.md`. | Was already tracked as PLANNED; not a new decision, just landed with one refinement (full invalidation vs. TTL-only) driven by this app's real-time nature. |
| 2026-08-09 | Backend grid clustering landed: `GET /v1/events/clusters?zoom=&bbox=`, `ST_SnapToGrid`-based, grid size a step function over `docs/product/VISION.md`'s 4 documented zoom/grid-size points, zoom≥17 returns individual unclustered markers per that doc. Meter→degree conversion uses a fixed Bangalore reference latitude — documented approximation, not geodetically exact. Shares `list-cache.ts`'s cache/invalidation with `GET /v1/events`. Surfaced a real test-hygiene issue: 49 rows orphaned by earlier failed test runs (before cleanup was hardened) contaminated a bbox-scoped cluster test — cleaned up, documented in `TESTING.md`. | VISION.md explicitly assigns clustering to the engine ("Backend grid clustering groups nearby events") — this was the last item in the current implementation round, closing out the vision-driven gap list. |
| 2026-08-09 | Confidence scoring landed: additive formula (+20/unique reporter, +30/evidence, −50/FRAUD, clamped 0–100), `Report`/`Evidence` rows linked on `POST /v1/events` when `reporterId`/`mediaUrls` given (both optional, `reporterId` unverified — no real accounts exist). `trustWeight` deliberately still not factored into the score (see `IMPLEMENTATION_NOTES.md`). Found and fixed a schema gap along the way: `Event`→`Report`/`Evidence` has no cascade delete, surfaced by a test's cleanup step, not by any production code path (nothing deletes events today). | Confirmed with user beforehand (additive formula over VISION.md's bucketed model) — not a new decision at implementation time, executing the earlier one. |
| 2026-08-09 | bbox/radius filtering landed on `GET /v1/events` (`events/geo-query.ts`, `ST_Within`/`ST_DWithin`). Two-step design (ID list, then normal Prisma pagination), not one combined raw-SQL query with cursor logic baked in — simpler, fine at current event volumes, documented as a known limitation if it needs to scale. `radiusKm` capped at 50 (Bangalore-only app). | Was already tracked as a Not-Yet-Built item; not a new decision, just landed. |
| 2026-08-09 | `/v1` route migration landed: all real API routes now under `/v1/` (`/health`, `/dev/*` deliberately excluded — infra/tooling, not contract). **No backward-compat aliases** for the old unversioned paths — removed outright. `routes/manifest.ts` is now consumed by `app.ts` (manifest-driven registration replaces hand-called `fastify.get/post/patch`). `packages/api-contracts`' generated types + `src/schemas.ts` are now the actual request-validation source in `app.ts`/`ingestion/`, resolving the enum-boundary deferral via `events/prisma-enum.ts`. Status transitions now validated against a state machine (409 on illegal, 404 on missing event), writes are compare-and-swap, and `updateStatus()` now broadcasts `EVENT_UPDATED`. | Completes work that was explicitly deferred, not a new architectural direction — see the two entries above and `IMPLEMENTATION_NOTES.md` for the concurrency/enum-boundary/versioning specifics. `public-map` is now stale against these paths until its own update lands. |

## What counts as "major" (needs your confirmation first)

- Adding a new datastore, message queue, or third-party infra vendor.
- Swapping a framework (Fastify, Next.js, Prisma, etc.) or the protobuf toolchain decided above.
- Splitting any module out into a separately-deployed service (breaks "modular monolith first").
- Introducing Kubernetes, microservices, Kafka, event sourcing, or CQRS.
- Adding an auth provider or changing the auth architecture (JWT vs. sessions vs. OAuth, etc.).
- Anything that changes a decision already logged above.

Everything else — new routes, new fields, new adapters following an already-agreed pattern, dependency bumps, bugfixes — proceeds without asking, documented as it happens.
