// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: plantir/transit/v1/get_fare_estimate.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { TransitMode, transitModeFromJSON, transitModeToJSON, transitModeToNumber } from "./arrival.js";

export const protobufPackage = "plantir.transit.v1";

export interface GetFareEstimateRequest {
  from: string;
  to: string;
  mode: TransitMode;
}

export interface GetFareEstimateResponse {
  fare: number;
  time: string;
}

function createBaseGetFareEstimateRequest(): GetFareEstimateRequest {
  return { from: "", to: "", mode: TransitMode.METRO };
}

export const GetFareEstimateRequest: MessageFns<GetFareEstimateRequest> = {
  encode(message: GetFareEstimateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.from !== "") {
      writer.uint32(10).string(message.from);
    }
    if (message.to !== "") {
      writer.uint32(18).string(message.to);
    }
    if (message.mode !== TransitMode.METRO) {
      writer.uint32(24).int32(transitModeToNumber(message.mode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFareEstimateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFareEstimateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.from = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.to = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.mode = transitModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFareEstimateRequest {
    return {
      from: isSet(object.from) ? globalThis.String(object.from) : "",
      to: isSet(object.to) ? globalThis.String(object.to) : "",
      mode: isSet(object.mode) ? transitModeFromJSON(object.mode) : TransitMode.METRO,
    };
  },

  toJSON(message: GetFareEstimateRequest): unknown {
    const obj: any = {};
    if (message.from !== "") {
      obj.from = message.from;
    }
    if (message.to !== "") {
      obj.to = message.to;
    }
    if (message.mode !== TransitMode.METRO) {
      obj.mode = transitModeToJSON(message.mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFareEstimateRequest>, I>>(base?: I): GetFareEstimateRequest {
    return GetFareEstimateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFareEstimateRequest>, I>>(object: I): GetFareEstimateRequest {
    const message = createBaseGetFareEstimateRequest();
    message.from = object.from ?? "";
    message.to = object.to ?? "";
    message.mode = object.mode ?? TransitMode.METRO;
    return message;
  },
};

function createBaseGetFareEstimateResponse(): GetFareEstimateResponse {
  return { fare: 0, time: "" };
}

export const GetFareEstimateResponse: MessageFns<GetFareEstimateResponse> = {
  encode(message: GetFareEstimateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fare !== 0) {
      writer.uint32(8).int32(message.fare);
    }
    if (message.time !== "") {
      writer.uint32(18).string(message.time);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFareEstimateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFareEstimateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.fare = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.time = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFareEstimateResponse {
    return {
      fare: isSet(object.fare) ? globalThis.Number(object.fare) : 0,
      time: isSet(object.time) ? globalThis.String(object.time) : "",
    };
  },

  toJSON(message: GetFareEstimateResponse): unknown {
    const obj: any = {};
    if (message.fare !== 0) {
      obj.fare = Math.round(message.fare);
    }
    if (message.time !== "") {
      obj.time = message.time;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFareEstimateResponse>, I>>(base?: I): GetFareEstimateResponse {
    return GetFareEstimateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFareEstimateResponse>, I>>(object: I): GetFareEstimateResponse {
    const message = createBaseGetFareEstimateResponse();
    message.fare = object.fare ?? 0;
    message.time = object.time ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
