// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: plantir/transit/v1/get_arrivals.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Arrival, TransitMode, transitModeFromJSON, transitModeToJSON, transitModeToNumber } from "./arrival.js";

export const protobufPackage = "plantir.transit.v1";

export interface GetArrivalsRequest {
  station: string;
  mode: TransitMode;
}

export interface GetArrivalsResponse {
  arrivals: Arrival[];
}

function createBaseGetArrivalsRequest(): GetArrivalsRequest {
  return { station: "", mode: TransitMode.METRO };
}

export const GetArrivalsRequest: MessageFns<GetArrivalsRequest> = {
  encode(message: GetArrivalsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.station !== "") {
      writer.uint32(10).string(message.station);
    }
    if (message.mode !== TransitMode.METRO) {
      writer.uint32(16).int32(transitModeToNumber(message.mode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetArrivalsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetArrivalsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.station = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mode = transitModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetArrivalsRequest {
    return {
      station: isSet(object.station) ? globalThis.String(object.station) : "",
      mode: isSet(object.mode) ? transitModeFromJSON(object.mode) : TransitMode.METRO,
    };
  },

  toJSON(message: GetArrivalsRequest): unknown {
    const obj: any = {};
    if (message.station !== "") {
      obj.station = message.station;
    }
    if (message.mode !== TransitMode.METRO) {
      obj.mode = transitModeToJSON(message.mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetArrivalsRequest>, I>>(base?: I): GetArrivalsRequest {
    return GetArrivalsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetArrivalsRequest>, I>>(object: I): GetArrivalsRequest {
    const message = createBaseGetArrivalsRequest();
    message.station = object.station ?? "";
    message.mode = object.mode ?? TransitMode.METRO;
    return message;
  },
};

function createBaseGetArrivalsResponse(): GetArrivalsResponse {
  return { arrivals: [] };
}

export const GetArrivalsResponse: MessageFns<GetArrivalsResponse> = {
  encode(message: GetArrivalsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.arrivals) {
      Arrival.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetArrivalsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetArrivalsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.arrivals.push(Arrival.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetArrivalsResponse {
    return {
      arrivals: globalThis.Array.isArray(object?.arrivals) ? object.arrivals.map((e: any) => Arrival.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetArrivalsResponse): unknown {
    const obj: any = {};
    if (message.arrivals?.length) {
      obj.arrivals = message.arrivals.map((e) => Arrival.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetArrivalsResponse>, I>>(base?: I): GetArrivalsResponse {
    return GetArrivalsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetArrivalsResponse>, I>>(object: I): GetArrivalsResponse {
    const message = createBaseGetArrivalsResponse();
    message.arrivals = object.arrivals?.map((e) => Arrival.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
