// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: plantir/transit/v1/arrival.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "plantir.transit.v1";

export enum TransitMode {
  METRO = "METRO",
  BUS = "BUS",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function transitModeFromJSON(object: any): TransitMode {
  switch (object) {
    case 0:
    case "METRO":
      return TransitMode.METRO;
    case 1:
    case "BUS":
      return TransitMode.BUS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TransitMode.UNRECOGNIZED;
  }
}

export function transitModeToJSON(object: TransitMode): string {
  switch (object) {
    case TransitMode.METRO:
      return "METRO";
    case TransitMode.BUS:
      return "BUS";
    case TransitMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function transitModeToNumber(object: TransitMode): number {
  switch (object) {
    case TransitMode.METRO:
      return 0;
    case TransitMode.BUS:
      return 1;
    case TransitMode.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum ArrivalStatus {
  ON_TIME = "ON_TIME",
  DELAYED = "DELAYED",
  APPROACHING = "APPROACHING",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function arrivalStatusFromJSON(object: any): ArrivalStatus {
  switch (object) {
    case 0:
    case "ON_TIME":
      return ArrivalStatus.ON_TIME;
    case 1:
    case "DELAYED":
      return ArrivalStatus.DELAYED;
    case 2:
    case "APPROACHING":
      return ArrivalStatus.APPROACHING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ArrivalStatus.UNRECOGNIZED;
  }
}

export function arrivalStatusToJSON(object: ArrivalStatus): string {
  switch (object) {
    case ArrivalStatus.ON_TIME:
      return "ON_TIME";
    case ArrivalStatus.DELAYED:
      return "DELAYED";
    case ArrivalStatus.APPROACHING:
      return "APPROACHING";
    case ArrivalStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function arrivalStatusToNumber(object: ArrivalStatus): number {
  switch (object) {
    case ArrivalStatus.ON_TIME:
      return 0;
    case ArrivalStatus.DELAYED:
      return 1;
    case ArrivalStatus.APPROACHING:
      return 2;
    case ArrivalStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface Arrival {
  id: string;
  route: string;
  direction: string;
  eta: string;
  status: ArrivalStatus;
  platform?: string | undefined;
}

function createBaseArrival(): Arrival {
  return { id: "", route: "", direction: "", eta: "", status: ArrivalStatus.ON_TIME, platform: undefined };
}

export const Arrival: MessageFns<Arrival> = {
  encode(message: Arrival, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.route !== "") {
      writer.uint32(18).string(message.route);
    }
    if (message.direction !== "") {
      writer.uint32(26).string(message.direction);
    }
    if (message.eta !== "") {
      writer.uint32(34).string(message.eta);
    }
    if (message.status !== ArrivalStatus.ON_TIME) {
      writer.uint32(40).int32(arrivalStatusToNumber(message.status));
    }
    if (message.platform !== undefined) {
      writer.uint32(50).string(message.platform);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Arrival {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArrival();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.route = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.direction = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.eta = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.status = arrivalStatusFromJSON(reader.int32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.platform = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Arrival {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      route: isSet(object.route) ? globalThis.String(object.route) : "",
      direction: isSet(object.direction) ? globalThis.String(object.direction) : "",
      eta: isSet(object.eta) ? globalThis.String(object.eta) : "",
      status: isSet(object.status) ? arrivalStatusFromJSON(object.status) : ArrivalStatus.ON_TIME,
      platform: isSet(object.platform) ? globalThis.String(object.platform) : undefined,
    };
  },

  toJSON(message: Arrival): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.route !== "") {
      obj.route = message.route;
    }
    if (message.direction !== "") {
      obj.direction = message.direction;
    }
    if (message.eta !== "") {
      obj.eta = message.eta;
    }
    if (message.status !== ArrivalStatus.ON_TIME) {
      obj.status = arrivalStatusToJSON(message.status);
    }
    if (message.platform !== undefined) {
      obj.platform = message.platform;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Arrival>, I>>(base?: I): Arrival {
    return Arrival.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Arrival>, I>>(object: I): Arrival {
    const message = createBaseArrival();
    message.id = object.id ?? "";
    message.route = object.route ?? "";
    message.direction = object.direction ?? "";
    message.eta = object.eta ?? "";
    message.status = object.status ?? ArrivalStatus.ON_TIME;
    message.platform = object.platform ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
